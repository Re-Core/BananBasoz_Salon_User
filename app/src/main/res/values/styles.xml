<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextLabel" parent="TextAppearance.AppCompat">
        <item name="android:textSize">20sp</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/textOnPink</item>
        <item name="colorPrimary">@color/textOnPink</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <item name="android:textColorHint">@color/textOnPink</item>
        <item name="colorControlNormal">@color/textOnPink</item>
        <item name="colorControlActivated">@color/textOnPink</item>
    </style>

    <style name="customBottomBar" parent="Widget.Design.BottomNavigationView">

        <item name="colorPrimary">@color/dark_brown</item>

    </style>

</resources>
